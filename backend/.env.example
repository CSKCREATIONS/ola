# Variables de entorno para backend

# Puerto del servidor Express
PORT=5000

# URI de MongoDB (atlas/local)
MONGODB_URI=mongodb://localhost:27017/pangea

# JWT
JWT_SECRET=cambialo_en_produccion
TOKEN_EXPIRATION=24h

# CORS (separadas por coma). Ej: http://localhost:3000,https://app.mi-dominio.com
CORS_ORIGINS=
